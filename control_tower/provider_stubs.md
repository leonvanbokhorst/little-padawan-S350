# Provider Stubs TODO

- BridgeClient: connect to WebSocket, authenticate via token, normalize events, expose command helpers. ✅
- VisionProvider: ingest RTSP frames, publish motion/person events. ✅ motion loop online.
- AudioProvider: run STT/TTS pipeline based on config.
- PersonaProvider: manage persona prompt, memory, and fallback responses.
