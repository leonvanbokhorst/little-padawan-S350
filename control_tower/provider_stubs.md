# Provider Stubs TODO

- BridgeClient: connect to WebSocket, authenticate via token, handle heartbeats.
- VisionProvider: ingest RTSP frames, publish motion/person events.
- AudioProvider: run STT/TTS pipeline based on config.
- PersonaProvider: manage persona prompt, memory, and fallback responses.
